apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: ledgerflow
spec:
  storageClassName: microk8s-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: ledgerflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      nodeSelector:
        kubernetes.io/hostname: microk8s-vm # Garante que o Pod seja agendado no nó específico
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "0.1"
              memory: "0.3Gi"
            limits:
              cpu: "0.2"
              memory: "0.6Gi"
          volumeMounts:
            - name: redis-storage
              mountPath: /opt/redis/data
      volumes:
        - name: redis-storage
          persistentVolumeClaim:
            claimName: redis-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ledgerflow
spec:
  selector:
    app: redis
  type: NodePort
  ports:
    - port: 2004
      targetPort: 6379
      nodePort: 30004